# Global settings
global:
  environment: production

# Docker images
images:
  backend:
    repository: ayaanshaheer/masters-abroad-backend
    tag: latest
    pullPolicy: Always
  frontend:
    repository: ayaanshaheer/masters-abroad-frontend
    tag: latest
    pullPolicy: Always
  postgres:
    repository: postgres
    tag: 15-alpine
    pullPolicy: IfNotPresent
  redis:
    repository: redis
    tag: 7-alpine
    pullPolicy: IfNotPresent
  qdrant:
    repository: qdrant/qdrant
    tag: latest
    pullPolicy: IfNotPresent

# Replica counts
replicaCount:
  backend: 2
  frontend: 2
  postgres: 1
  redis: 1
  qdrant: 1

# Service configurations
service:
  frontend:
    type: LoadBalancer
    port: 80
  backend:
    type: ClusterIP
    port: 8000
  postgres:
    type: ClusterIP
    port: 5432
  redis:
    type: ClusterIP
    port: 6379
  qdrant:
    type: ClusterIP
    ports:
      http: 6333
      grpc: 6334

# Storage
persistence:
  postgres:
    size: 1Gi
  redis:
    size: 500Mi
  qdrant:
    size: 1Gi

# Application secrets 
secrets:
  postgresPassword: "postgrespassword"
  redisPassword: "redispassword"
  secretKey: "secretkey"
  groqApiKey: "groqapikey"

# ConfigMap data
config:
  postgresDb: "masters_abroad"
  postgresUser: "postgres"
